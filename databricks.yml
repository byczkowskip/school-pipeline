bundle:
  name: school_pipeline

artifacts:
  school_pipeline:
    type: whl
    build: python setup.py bdist_wheel

resources:
  jobs:
    bronze_loader:
      name: bronze_loader_${bundle.target}
      tasks:
        - task_key: bronze
          python_wheel_task:
            package_name: school_pipeline
            entry_point: bronze_loader
          existing_cluster_id: "0319-183230-ilr9d2uq"

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-7405608923276884.4.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-7405608923276884.4.azuredatabricks.net
      root_path: /Workspace/Users/piotr@byczkowskipgmail.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: piotr@byczkowskipgmail.onmicrosoft.com
        level: CAN_MANAGE